/* Optional: paste your Firebase web-app config here and commit it, so
   teammates only need the team code (they never paste the config).
   This file is SAFE to publish — the config is not a secret; your data
   is protected by the Firestore rules + the random team code.
   See README → "Team sync" for the 15-minute setup.

   Replace `null` with the object from the Firebase console, e.g.:

   window.FIREBASE_CONFIG = {
     apiKey: "AIza…",
     authDomain: "bd-delivery-sync.firebaseapp.com",
     projectId: "bd-delivery-sync",
     storageBucket: "bd-delivery-sync.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456789:web:abcdef"
   };
*/
window.FIREBASE_CONFIG = null;
